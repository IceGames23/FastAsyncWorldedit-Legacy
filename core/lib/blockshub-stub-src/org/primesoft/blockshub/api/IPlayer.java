/*
 * Minimal compile-only API stub for BlocksHub v2.0 — see IBlocksHubApi.java.
 * FAWE only uses IPlayer as an opaque type (never calls its methods), so an
 * empty marker interface is sufficient for compilation.
 */
package org.primesoft.blockshub.api;

public interface IPlayer {
}
